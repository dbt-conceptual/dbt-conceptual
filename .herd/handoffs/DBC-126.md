# Handoff: DBC-126 — feat(mcp): herd_spawn — full agent spawn flow in one call

## What was done
- **Enhanced `.herd/mcp/src/herd_mcp/tools/spawn.py`**:
  - Added `ticket_id` optional parameter to `execute()` function
  - Implemented ticket-based spawn path that creates worktree, registers instance, transitions ticket, and assembles context
  - Added 5 helper functions: `_find_repo_root()`, `_create_worktree()`, `_read_file_safe()`, `_extract_craft_section()`, `_assemble_context_payload()`
  - Preserved legacy multi-spawn mode for backward compatibility
  - Integrated with Linear API for ticket auto-registration and state sync

- **Enhanced `.herd/mcp/tests/test_spawn.py`**:
  - Added 6 new tests covering ticket-based spawn functionality
  - Tests cover: worktree creation, context assembly, Linear auto-registration, Linear sync, error handling
  - Added test for craft section extraction logic
  - All 14 tests passing (8 legacy + 6 new)

## What to verify (for Wardenstein)

### Core Functionality
- Verify `herd_spawn` with `ticket_id` parameter creates worktree at correct path (`/private/tmp/{agent}-{ticket-id}`)
- Verify worktree is created on correct branch (`herd/{agent}/{ticket-id-lowercase}-herd-spawn`)
- Verify agent instance is registered in DuckDB with ticket linkage
- Verify ticket transitions to "in_progress" in both DuckDB and Linear

### Context Assembly
- Verify context payload includes all required sections:
  - Role file content
  - Agent-specific craft standards (not full craft.md)
  - CLAUDE.md content
  - Slack token
  - Branch protection rules
  - Ticket details from Linear
  - Working directory and branch info
- Verify craft section extraction correctly filters by agent

### Edge Cases
- Verify behavior with non-existent ticket (should auto-register from Linear if valid Linear ID)
- Verify behavior with ticket not in Linear or DuckDB (should return error)
- Verify worktree creation failure is handled gracefully
- Verify backward compatibility: calling without `ticket_id` works as before

### Integration Points
- Verify Linear API integration: auto-registration and state transitions
- Verify git subprocess calls don't leave orphan worktrees on failure
- Verify file reading handles missing files gracefully

### Test Quality
- Verify test coverage >80% on changed files
- Verify all 14 tests pass
- Verify tests properly mock external dependencies (git, Linear API, filesystem)

## What to document (for Shakesquill)
- **MCP tool signature change**: `herd_spawn` now accepts optional `ticket_id` parameter
- **New spawn workflow**: When `ticket_id` is provided, the tool creates worktree, registers instance, transitions ticket, and returns assembled context payload
- **Context payload structure**: Document what's included in the returned `context_payload` field
- **Backward compatibility**: Legacy multi-spawn mode (without `ticket_id`) still works as before
- **Linear integration**: Tickets auto-register from Linear if not in DuckDB
- **State transitions**: Tickets automatically move to "In Progress" on spawn

## Open questions for Architect
- Should we add a config option for worktree base path (currently hardcoded to `/private/tmp/`)?
- Should we make the branch name pattern configurable (currently `herd/{agent}/{ticket-id}-herd-spawn`)?
- Should we handle existing worktrees (currently will fail if worktree already exists)?
- Should Linear sync failures be hard errors or just warnings (currently warnings)?

# Handoff: DBC-110, DBC-111, DBC-112 — Schema Refinements

**PR:** https://github.com/dbt-conceptual/dbt-conceptual/pull/122
**Branch:** `herd/grunt/dbc-110-112-schema-refinements`
**Tickets:**
- https://linear.app/dbt-conceptual/issue/DBC-110
- https://linear.app/dbt-conceptual/issue/DBC-111
- https://linear.app/dbt-conceptual/issue/DBC-112

## What Was Done

### DBC-110: Finding Pattern & Outcome Tracking

**Schema Changes:**
- Added `finding_pattern_id TEXT` to `herd.review_finding`
- Added `finding_outcome TEXT` to `herd.review_finding`
- Updated both `init_schema.sql` and `schema.sql`

**dbt Model Updates:**
- `stg_herd__review_finding.sql` — added new columns to staging
- `sn_review_finding.sql` — added new columns to satellite
- `fact_review_quality.sql` — added aggregated metrics:
  - `unique_pattern_count` — count of distinct patterns
  - `accepted_finding_count` — findings marked accepted
  - `disputed_finding_count` — findings marked disputed
  - `deferred_finding_count` — findings marked deferred

### DBC-111: Agent Observation Table

**New Table:** `herd.agent_observation`
- Tracks agent self-observations and pattern recognition
- Columns: observation_id (PK), agent_code, session_code, observation_type, observation_content, confidence, created_at, deleted_at

**Full dbt Model Stack:**
- **Staging:** `stg_herd__agent_observation` (3 files: .sql, .yml, .md)
- **Vault Hub:** `h_observation` (3 files)
- **Vault Satellite:** `s_observation_base` (3 files) — Type 2 SCD with hashdiff
- **Mart Dimension:** `dim_agent_observation` (3 files) — Type 2 SCD with valid_from/valid_to
- **Source:** Updated `source.herd.yml` with new table

### DBC-112: Decision Record Table

**New Table:** `herd.decision_record`
- Tracks architectural and operational decisions
- Columns: decision_id (PK), decision_type, context, decision, rationale, alternatives_considered, decided_by, ticket_code, created_at, deleted_at

**Full dbt Model Stack:**
- **Staging:** `stg_herd__decision_record` (3 files: .sql, .yml, .md)
- **Vault Hub:** `h_decision` (3 files)
- **Vault Satellite:** `s_decision_base` (3 files) — Type 2 SCD with hashdiff
- **Mart Dimension:** `dim_decision_record` (3 files) — Type 2 SCD with valid_from/valid_to
- **Source:** Updated `source.herd.yml` with new table

## Files Changed

**Schema DDL (2 files):**
- `.herd/dbt/init_schema.sql` — added 2 columns to review_finding, 2 new tables
- `.herd/mcp/src/herd_mcp/schema.sql` — same changes (DuckDB-compatible version)

**Bronze Layer (1 file):**
- `.herd/dbt/models/bronze/source.herd.yml` — added agent_observation, decision_record

**Silver Layer - Staging (8 files):**
- `stg_herd__review_finding.sql` (updated)
- `stg_herd__agent_observation.{sql,yml,md}` (new)
- `stg_herd__decision_record.{sql,yml,md}` (new)

**Silver Layer - Vault (13 files):**
- `sn_review_finding.sql` (updated)
- `h_observation.{sql,yml,md}` (new)
- `s_observation_base.{sql,yml,md}` (new)
- `h_decision.{sql,yml,md}` (new)
- `s_decision_base.{sql,yml,md}` (new)

**Gold Layer - Mart (7 files):**
- `fact_review_quality.sql` (updated)
- `dim_agent_observation.{sql,yml,md}` (new)
- `dim_decision_record.{sql,yml,md}` (new)

**Total:** 29 files changed (5 updated, 24 new)

## What to Verify

### Schema Verification
1. **SQL Syntax:** Both schema files should have valid SQL
2. **Column Consistency:** `init_schema.sql` uses NOT NULL constraints; `schema.sql` is nullable
3. **Table Count:** Schema now has 28 tables (was 26)

### DBC-110 Verification
1. **Staging Model:** `stg_herd__review_finding` includes finding_pattern_id and finding_outcome
2. **Satellite Model:** `sn_review_finding` includes both new columns
3. **Fact Model:** `fact_review_quality` aggregates:
   - COUNT(DISTINCT finding_pattern_id) → unique_pattern_count
   - SUM(CASE finding_outcome = 'accepted') → accepted_finding_count
   - SUM(CASE finding_outcome = 'disputed') → disputed_finding_count
   - SUM(CASE finding_outcome = 'deferred') → deferred_finding_count

### DBC-111 Verification (agent_observation)
1. **Staging:** stg_herd__agent_observation pulls from source, sets is_deleted flag
2. **Hub:** h_observation generates observation_tk from observation_id
3. **Satellite:** s_observation_base historizes with hashdiff, incremental load
4. **Dimension:** dim_agent_observation creates Type 2 SCD with valid_from/valid_to
5. **YAML:** All .yml files have complete column descriptions
6. **Markdown:** All .md files have brief descriptions

### DBC-112 Verification (decision_record)
1. **Staging:** stg_herd__decision_record pulls from source, sets is_deleted flag
2. **Hub:** h_decision generates decision_tk from decision_id
3. **Satellite:** s_decision_base historizes with hashdiff, incremental load
4. **Dimension:** dim_decision_record creates Type 2 SCD with valid_from/valid_to
5. **YAML:** All .yml files have complete column descriptions
6. **Markdown:** All .md files have brief descriptions

### Pattern Consistency Checks
1. **Naming:** All models follow existing patterns (stg_herd__, h_, s_, dim_)
2. **Incremental Logic:** All incremental models use proper WHERE clauses
3. **Hash Functions:** generate_hash() for technical keys, hashdiff() for change detection
4. **SCD Logic:** LEAD() window function for valid_to, COALESCE to '9999-12-31'
5. **dbt_utils:** Type 2 dimensions use dbt_utils.generate_surrogate_key()

### Potential Issues
1. **dbt_utils dependency:** Mart models use `{{ dbt_utils.generate_surrogate_key() }}` — verify package is installed
2. **Incremental materialization:** All vault/mart models should build incrementally
3. **Gitignore:** New files had to be force-added (git add -f) due to .herd/ ignore patterns

## Test Execution
Not executed. Wardenstein should:
1. Validate SQL syntax in both schema files
2. Check dbt model compilation
3. Verify YAML schema completeness
4. Confirm naming consistency with existing models

## Notes
- Schema tables now: 28 (12 entity defs + 3 versioned content + 3 skills + 1 instance + 2 junctions + 5 activity ledgers + 2 reflexive learning)
- All new models follow existing vault modeling patterns (hub/satellite for Type 2 SCD)
- Both new dimensions are Type 2 SCD to preserve historical changes
- Finding pattern/outcome columns are nullable (for backward compatibility)

**For the Herd!**
